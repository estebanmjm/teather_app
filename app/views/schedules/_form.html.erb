<%= form_for(@schedule) do |f| %>
  <% if @schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% @schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :initial_day %><br>
    <%= f.datetime_select :initial_day %>
  </div>
  <div class="field">
    <%= f.label :last_day %><br>
    <%= f.datetime_select :last_day %>
  </div>
  <div class="field">
    <%= f.label :play_id %><br>
    <%= f.number_field :play_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
